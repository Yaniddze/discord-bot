import { availableRoles } from '../utils/constants';
import { createDefaultMenuSelector } from '../utils/defaultMenuSelector';

export default createDefaultMenuSelector('setMainRoles', availableRoles);

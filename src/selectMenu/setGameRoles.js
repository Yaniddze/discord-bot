import { availableGames } from '../utils/constants';
import { createDefaultMenuSelector } from '../utils/defaultMenuSelector';

export default createDefaultMenuSelector('setGameRoles', availableGames);
